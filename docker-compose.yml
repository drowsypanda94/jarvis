services:
  jarvis:
    image: python:3.11-slim
    container_name: jarvis
    working_dir: /app
    ports:
      - "3000:3000"
      - "8741:8741"
    env_file:
      - .env
    restart: unless-stopped
